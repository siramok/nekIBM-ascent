-
  action: "add_triggers"
  triggers:
    t1:
      params:
        condition: "cycle() % 4 == 0"
        actions_file : "binning.yaml"
    t1:
      params:
        condition: "cycle() % 5 == 0"
        actions_file : "jupyter_interface.yaml"
