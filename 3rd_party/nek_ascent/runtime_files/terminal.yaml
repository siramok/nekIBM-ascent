-
  action: "add_triggers"
  triggers:
    t1:
      params:
        condition: "cycle() % 4 == 0"
        actions_file : "binning.yaml"
    t2:
      params:
        condition: "cycle() % 5 == 0"
        actions_file : "terminal_interface.yaml"
