#include "Bridge.h"
#include <iostream>

void ascent_setup()
{
    std::cout << "Test of ascent_setup()" << std::endl;
    std::cin.get();
}

void ascent_update()
{
    std::cout << "Test of ascent_update()" << std::endl;
    std::cin.get();
}

void ascent_finalize()
{
    std::cout << "Test of ascent_finalize()" << std::endl;
    std::cin.get();
}